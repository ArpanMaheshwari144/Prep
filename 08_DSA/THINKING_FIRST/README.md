# DSA — THINKING-FIRST (active derivation, fresh start)

> **Goal:** RECOGNITION already strong (479 problems done) → ab **GENERATION** (khud derive) build karna.
> Passive (tutorial dekh ke) se thinking nahi bani — ab ACTIVE.

## METHOD (Arpan's — Claude sirf on-demand)

```
   1. Problem lo (NeetCode 150 / Striver SDE — Arrays & Hashing se shuru).
   2. COPY pe KHUD solve karo:
        - kya poochh raha (apne words)
        - brute force → optimize → kaunsa pattern
        - poori LOGIC apne words mein likho
        - 10-15 min real struggle
   3. Claude CHUP rahega — sirf tab bolega jab Arpan KHUD hint maange / fass ke poochhe.
        (warna kuch nahi — na solution, na hint)
   4. Solve hone ke baad → Arpan batayega kaise kiya (yahan code likhe ya chat mein).
   5. Pattern navigator mein note: signal -> pattern -> trick.
```

## RULES
```
   - Solution PEHLE mat dekho. Stuck (15 min) → sirf KEY IDEA dekho (full code nahi) → band → re-derive.
   - Recall != Derive. Khud nikaal paye to thinking ban rahi.
   - Daily 2-3 problem, ACTIVE, fresh dimaag se.
   - Purana 08_DSA = foundation/reference (deleted nahi — neenv hai).
```
