# ============================================================
# NOTE: is service ki config ab application.yml me hai (isi folder me).
# gateway ke ROUTES (order/payment pe forward) YAML me saaf likhte -> isliye .yml use kiya.
# neeche wali line ab .yml me hai -> yahan comment kar di (delete nahi).
# ============================================================
# spring.application.name=api-gateway
